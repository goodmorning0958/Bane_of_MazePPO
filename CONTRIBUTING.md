For Contributors:

To edit or create new code files:
 - Create a new file: [file_name].[file_extension], or edit an existing one
 - Once you paste in the code/contents, run (INSIDE A TERMINAL/CODESPACE): git add . ; git commit -m "Update Bane_of_mazePPO" ; git push 
 - eat me

You can also do this in VS Code by:
 - clone the repo
 - make a folder called Bane_of_mazePPO, and insert your contents.
 - make vs_code be in the Bane_of_mazePPO folder on your computer
 - To add changes: run git add . ; git commit -m "Update Bane_of_mazePPO" ; git push
  
